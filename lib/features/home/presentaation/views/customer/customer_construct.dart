import 'package:flutter/material.dart';

class CustomerConstruct extends StatelessWidget {
  const CustomerConstruct({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Constructview'),);
  }
}