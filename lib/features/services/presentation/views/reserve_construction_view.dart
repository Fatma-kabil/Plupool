import 'package:flutter/material.dart';

class ReserveConstructionView extends StatelessWidget {
  const ReserveConstructionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: ListView(children: [
        SizedBox(height: 39,)
      ])),
    );
  }
}
