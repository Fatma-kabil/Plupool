import 'package:flutter/material.dart';

class CustomerStoreView extends StatelessWidget {
  const CustomerStoreView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('StoreView'));
  }
}
