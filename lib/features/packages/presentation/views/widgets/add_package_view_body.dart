import 'package:flutter/material.dart';

class AddPackageViewBody extends StatelessWidget {
  const AddPackageViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}