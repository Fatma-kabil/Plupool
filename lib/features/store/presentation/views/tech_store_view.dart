import 'package:flutter/material.dart';

class TechStoreView extends StatelessWidget {
  const TechStoreView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}