import 'package:flutter/material.dart';

class CustomerProfilrView extends StatelessWidget {
  const CustomerProfilrView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}