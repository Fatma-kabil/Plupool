import 'package:flutter/material.dart';

class StoreOrderViewBody extends StatelessWidget {
  const StoreOrderViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomScrollView(slivers: []);
  }
}
